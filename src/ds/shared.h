#pragma once
// shortcut for template<typename T>, because it draws way too much attention
#define t_ template<typename T>
// Type of element + Comparator function / lambda
#define tc_ template<typename T, typename C>
